<!DOCTYPE html>
<html>
<head>
  <title>Редактирование - <%= @company.company_name %></title>
  <meta charset="utf-8">
  <style>
    body { font-family: Arial, sans-serif; margin: 20px; }
    .form-container { background: #f8f9fa; padding: 20px; border-radius: 5px; max-width: 600px; }
    .form-group { margin-bottom: 20px; }
    .form-group label { 
      display: block; 
      margin-bottom: 5px; 
      font-weight: bold; 
      color: #333; 
    }
    .form-group input[type="checkbox"] { 
      margin-right: 8px; 
      transform: scale(1.2);
    }
    .checkbox-container {
      display: flex;
      align-items: center;
      padding: 10px;
      background: white;
      border: 1px solid #ddd;
      border-radius: 4px;
    }
    .checkbox-container label {
      margin: 0;
      font-weight: normal;
    }
    .actions { margin-top: 30px; }
    .actions button, .actions a { 
      display: inline-block; 
      padding: 10px 20px; 
      text-decoration: none; 
      border-radius: 5px; 
      margin-right: 10px; 
      border: none;
      cursor: pointer;
      font-size: 14px;
    }
    .btn-primary { background: #007bff; color: white; }
    .btn-primary:hover { background: #0056b3; }
    .btn-secondary { background: #6c757d; color: white; }
    .btn-secondary:hover { background: #545b62; }
    .company-name { color: #666; margin-bottom: 20px; }
    .notice { 
      background: #d4edda; 
      color: #155724; 
      padding: 10px; 
      border-radius: 4px; 
      margin-bottom: 20px; 
      border: 1px solid #c3e6cb;
    }
  </style>
</head>
<body>
  <h1>Редактирование компании</h1>
  
  <div class="company-name">
    <strong><%= @company.company_name %></strong> (<%= @company.email %>)
  </div>

  <% if notice %>
    <div class="notice"><%= notice %></div>
  <% end %>

  <div class="form-container">
    <%= form_with model: @company, url: admin_panel_path(@company), method: :patch, local: true do |form| %>
      
      <div class="form-group">
        <label>Верификация партнера</label>
        <div class="checkbox-container">
          <%= form.check_box :is_partner_verified %>
          <%= form.label :is_partner_verified, "Партнер верифицирован" %>
        </div>
      </div>

      <div class="form-group">
        <label>Верификация телефона</label>
        <div class="checkbox-container">
          <%= form.check_box :is_phone_verified %>
          <%= form.label :is_phone_verified, "Телефон верифицирован" %>
        </div>
      </div>

      <div class="actions">
        <%= form.submit "Сохранить изменения", class: "btn-primary" %>
        <%= link_to "Отмена", admin_panel_path(@company), class: "btn-secondary" %>
      </div>
    <% end %>
  </div>
</body>
</html>