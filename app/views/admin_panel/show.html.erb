<!DOCTYPE html>
<html>
<head>
  <title>Компания - <%= @company.company_name %></title>
  <meta charset="utf-8">
  <style>
    body { font-family: Arial, sans-serif; margin: 20px; }
    .company-info { background: #f8f9fa; padding: 20px; border-radius: 5px; margin-bottom: 20px; }
    .info-row { margin-bottom: 10px; }
    .label { font-weight: bold; color: #333; }
    .value { margin-left: 10px; }
    .status { padding: 4px 8px; border-radius: 4px; color: white; font-size: 12px; }
    .verified { background-color: #28a745; }
    .not-verified { background-color: #dc3545; }
    .actions { margin-top: 20px; }
    .actions a { 
      display: inline-block; 
      padding: 10px 15px; 
      background: #007bff; 
      color: white; 
      text-decoration: none; 
      border-radius: 5px; 
      margin-right: 10px; 
    }
    .actions a:hover { background: #0056b3; }
    .actions .back { background: #6c757d; }
    .actions .back:hover { background: #545b62; }
  </style>
</head>
<body>
  <h1>Информация о компании</h1>

  <div class="company-info">
    <div class="info-row">
      <span class="label">ID:</span>
      <span class="value"><%= @company.id %></span>
    </div>

    <div class="info-row">
      <span class="label">Название компании:</span>
      <span class="value"><%= @company.company_name %></span>
    </div>

    <div class="info-row">
      <span class="label">Email:</span>
      <span class="value"><%= @company.email %></span>
    </div>

    <div class="info-row">
      <span class="label">Регион:</span>
      <span class="value"><%= @company.city_name %></span>
    </div>

    <div class="info-row">
      <span class="label">Телефон 1:</span>
      <span class="value"><%= @company.phone_1 %></span>
    </div>

    <div class="info-row">
      <span class="label">Телефон 2:</span>
      <span class="value"><%= @company.phone_2 %></span>
    </div>

    <div class="info-row">
      <span class="label">Партнер верифицирован:</span>
      <span class="value">
        <span class="status <%= @company.is_partner_verified? ? 'verified' : 'not-verified' %>">
          <%= @company.is_partner_verified? ? 'Да' : 'Нет' %>
        </span>
      </span>
    </div>

    <div class="info-row">
      <span class="label">Телефон верифицирован:</span>
      <span class="value">
        <span class="status <%= @company.is_phone_verified? ? 'verified' : 'not-verified' %>">
          <%= @company.is_phone_verified? ? 'Да' : 'Нет' %>
        </span>
      </span>
    </div>

    <div class="info-row">
      <span class="label">Дата регистрации:</span>
      <span class="value"><%= @company.created_at.strftime("%d.%m.%Y %H:%M") %></span>
    </div>

    <div class="info-row">
      <span class="label">Роль:</span>
      <span class="value"><%= @company.role.humanize %></span>
    </div>

    <div class="info-row">
      <span class="label">Количество машин:</span>
      <span class="value"><%= @company.cars.count %></span>
    </div>
  </div>

  <div class="actions">
    <%= link_to "Редактировать", edit_admin_panel_path(@company) %>
    <%= link_to "Назад к списку", admin_panel_index_path, class: "back" %>
  </div>
</body>
</html>